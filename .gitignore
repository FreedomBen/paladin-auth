TODO.md
.claude/settings.local.json
.codex

# Rust workspace artifacts
/target
**/*.rs.bk
Cargo.lock.bak

# tempfile::TempDir scratch dirs that leak into the project root when
# the shell's TMPDIR points here. Tests should never deposit scratch in
# the workspace — see crates/*/tests/common/mod.rs `test_tempdir()`.
.tmp*/
